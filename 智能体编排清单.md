# 智能体编排 · 全流程梳理（2026-07-30）

> 目的：把全站所有 AI 生成功能盘成"智能体清单"，逐个记录现状（功能 / 前置 / 产出 / next），
> 再逐个给出接入智能体编排（最小集）的方案。**本文档只梳理，不改代码。**
>
> 名词约定：
> - **最小集（unit）**：`/admin/wf` 画布上的一张编排卡，config 五段 = 开始 / 前置 / 运行 / 输出产物 / next（`agent_unit.py`）。
> - **Step**：`steps.py` 任务队列里注册的生成步骤（19 个），有 before / run / next 三段和依赖派发。
> - **API 直调**：装配逻辑写在 API 端点里、不走任务队列的生成（接入编排的主要缺口来源）。
> - 状态标记：✅ 已有最小集｜⚠️ 有 Step 无最小集 / 最小集不完整｜❌ 无 Step 无最小集（API 直调或功能缺失）。

---

## 一、智能体清单总表（按生产流程）

| # | 智能体 | 页面入口 | 现有实现 | 已有最小集 | 状态 |
|---|---|---|---|---|---|
| **项目筹备（总览页）** |
| 1 | 项目信息智能体 | 建项目自动 / 总览自动补拟 | Step `gen_project_info` | `a.project-info` | ✅ |
| 2 | 架构大纲智能体 | 总览「生成大纲 / 重新生成大纲」 | Step `gen_outline_md` | `a.project-outline` | ✅ |
| 3 | 要素类型判定智能体 | 素材库自动判定 | Step `gen_element_kinds` | `a.element-kinds` | ✅ |
| 4 | 封面智能体（关键视觉蒸馏 + 出图） | 总览封面画布 | API 直调（projects.py） | 无 | ❌ |
| 5 | 项目视觉锚点智能体（三张） | 总览「依次生成三张」 | API 直调（project_visual_assets.py） | 无 | ❌ |
| 6 | 先导预告片智能体（蒸馏 + 视频） | 总览「重新生成」 | API 直调（trailer.py + asset_gen.py） | 无 | ❌ |
| **核心要素（素材库页）** |
| 7 | 核心要素抽取智能体（角色/场景/道具批量建档） | 素材库「AI 生成素材设定」 | API 直调流式（pipeline_stream） | 无 | ❌ |
| 8 | 要素档案智能体（结构化档案 + 外貌提示词） | 角色卡「AI 补全档案」「补全缺档(N)」 | API 直调（element_profile.py），被 Step 隐式调用 | 无 | ❌ |
| 9 | 角色设定图智能体 | 素材库「生成角色设定图(三视图)」 | Step `gen_element_sheet` | `a.character-sheet` | ✅ |
| 10 | 场景设定图智能体 | 素材库「生成场景设定图」 | Step `gen_element_sheet`（同一 Step） | `a.scene-sheet` | ✅ |
| 11 | 道具设定图智能体 | 素材库（道具类要素） | Step `gen_element_sheet`（同一 Step） | 无独立卡（可复用 9/10 模式） | ⚠️ |
| 12 | 音色设计智能体（捏音色 / 批量选角） | 素材库角色卡「捏音色」 | API 直调（voice_casting.py） | 无 | ❌ |
| 13 | 音色样本智能体（试听样本 / 情绪小样） | 管理后台音色库 | Step `gen_voice_samples` + API 直调（情绪小样） | `a.voice-sample` | ✅ |
| 14 | 自由素材生成智能体（素材图 / 素材视频） | 素材库「生成图片 / 生成视频」 | API 直调（asset_gen.py） | 无 | ❌ |
| **剧集（剧集页）** |
| 15 | 剧集目录智能体（生成 / 续写章节：标题+简介，1 或多章） | 剧集「新增章节 → 开始续写」 | API 直调流式（pipeline.build_outline / append_chapters） | 无 | ❌ |
| 16 | 正文智能体（含目标时长 6–10 分钟） | 剧集「生成正文 / 重写正文」 | API 直调流式（pipeline.write_chapter） | 无 | ❌ |
| **拆镜与章级资产（视频页）** |
| 17 | 拆镜智能体（章 → 分镜骨架） | 视频页「动态生成分镜」 | Step `breakdown_chapter` | `a.chapter-breakdown` | ✅ |
| 18 | 详细分镜展开智能体 | 自动串（拆镜后） | Step `expand_shot_details` | `a.chapter-expand` | ✅ |
| 19 | 场景空间规划智能体（场景光影站位锚定文字） | 「重跑场景空间规划」 | Step `scene_blocking` | `a.chapter-blocking` | ✅ |
| 20 | 空场景基准图智能体（第一阶段，无人） | 场景组「出基准图」 | Step `gen_scene_empty` | `a.scene-empty` | ✅ |
| 21 | 场景站位图智能体（第二阶段，加人） | 场景组「生成」 | Step `gen_scene_sheet` | `a.scene-blocking` | ✅ |
| 22 | 整集场景组图智能体（批量，只补缺） | 「批量场景」 | Step `gen_scene_sheets_group` | `c.episode-scenes` | ✅ |
| 23 | 分镜定格选取智能体（first/key/last） | （画布环节节点） | Step `gen_stills` | `a.chapter-stills` | ✅ |
| 24 | 宫格故事板智能体（章级总览图） | 分镜图画布「生成故事板」 | Step `gen_overview_grid` | `a.chapter-grid` | ✅ |
| **单镜生产（视频页 · 镜级）** |
| 25 | 分镜剧本智能体（cuts 时间轴 / 镜头设计） | 「重做镜头设计」/ 画布节点 | Step `gen_shot_storyboard` | `a.shot-storyboard` | ✅ |
| 26 | 提示词装配+质检智能体（图/视频三套 + 九维质检） | 画布节点 | Step `gen_prompts` | `a.shot-prompt` | ✅ |
| 27 | 首帧智能体（单镜） | 「生成首图」 | Step `gen_keyframe` | `a.shot-keyframe` | ✅ |
| 28 | 批量首帧组图智能体（按场景组切批，只补缺） | 「批量首帧 → 全部生成」 | Step `gen_keyframes_group` | `c.episode-keyframes` | ✅ |
| 29 | 尾帧提示词智能体 | （端点已有，前端未接线） | API 直调（shots.py last-prompt/ai，落库） | 无 | ❌ |
| 30 | 尾帧智能体 | 尾帧卡画布 | Step `gen_last_keyframe` | `a.shot-lastframe` | ✅ |
| 31 | 分镜视频智能体（单镜 / 整集批量） | 「生成视频」 | Step `gen_video` | `a.shot-video` / `c.episode-video` | ✅ |
| **整集母带实验链（视频页 · 更多菜单）** |
| 32 | 母带提示词智能体（6 模式蒙太奇蒸馏） | 「母带实验提示词」 | API 直调（episode_flash / episode_adaptive） | 无 | ❌ |
| 33 | 母带视频智能体（分组并发 + ffmpeg 拼接） | 「按当前提示词生成母带」 | API 直调（shots.py + video_concat） | 无 | ❌ |
| 34 | 母带抽帧（非 AI，ffmpeg 按节拍抽） | 「抽取关键帧」 | API 直调（video_frame_extract） | 无 | ❌（可作为工具而非智能体） |
| 35 | 双时间线 → 转正式分镜 | 剪辑台「转为草稿分镜」 | API 直调（episode_preview_timeline） | 无 | ❌ |
| **成片** |
| 36 | 成片合成智能体（分镜视频 → 整集） | **无入口** | **功能缺失**（仅有内部函数 `video_concat.concat_video_attachments`） | 无 | ❌ 功能本身缺失 |
| **旁路 / 辅助（不是独立产线节点，但编排要能引用）** |
| 37 | 提示词 AI 改写（image/video/last/封面/要素外貌） | 各画布「AI 改写」 | API 直调，多数不落库 | 无 | ❌（建议保留人工旁路，不进编排） |
| 38 | 画布助手（canvas agent / canvas skills） | 无限画布「让助手处理」 | canvas_skills.py | 无 | 独立体系，暂不合并 |
| 39 | 组合编排（已有）：`c.project-bootstrap` / `c.chapter-to-storyboard` / `c.episode-elements` | — | 递归 ref_flows | 已有 | ✅ |
| 40 | 取数件（已有）：`q.project-info` / `q.element` / `q.chapter-shots` / `q.scene-groups` / `q.shot-elements` | — | 前置取数 | 已有 | ✅ |

**小结：产线主干（拆镜 → 场景 → 首帧 → 视频）已全部有最小集；缺口集中在三块：**
1. **项目筹备的视觉资产**（封面 / 视觉锚点 / 预告片）——全是 API 直调；
2. **文字产线上游**（要素抽取 / 档案 / 剧集目录 / 正文）——全是 API 直调流式；
3. **整集母带实验链与成片合成**——前者全在 API 端点，后者功能本身缺失。

---

## 二、逐个调研 + 接入方案

> 每条含：**现状**（功能 / 实现 / 触发 / 前置 / 产出 / next）与**接入**（如何进最小集五段）。
> 已有最小集的条目只列"核对要点"；缺失的条目给出接入路径。
> 接入路径通则见第三节"两条标准路径"。

### A. 项目筹备

#### 1. 项目信息智能体 ✅ `a.project-info`
- **现状**：Step `gen_project_info`（steps.py:2296，group=筹备）。建项目后自动入队；只填用户留空的字段。前置：项目存在。产出：`content_projects.title/synopsis/writing_style/art_style/storyline + config.genre/suggested_chapters`。next：`chains_to=[gen_outline_md]`（无大纲时）。
- **接入**：已完成（unit 配 `step=gen_project_info`）。核对：unit 的 `then_flows` 是否与 Step 自身 `chains_to` 重复触发大纲（会因 enqueue 幂等去重兜住，但语义上应二选一）。

#### 2. 架构大纲智能体 ✅ `a.project-outline`
- **现状**：Step `gen_outline_md`（steps.py:2347，timeout 900），内含内容质检+改稿闭环（`review_outline_development`）。旧稿自动归档进 `project_materials`。另有同步 / SSE 流式两个 API 直调变体（前端「重新生成大纲」走 task 版）。前置：项目基本信息。产出：`content_projects.outline_md + config.outline_content_review`。next：`chains_to=[gen_element_kinds]`。
- **接入**：已完成。注意：流式变体（`/outline-md/stream`）是纯前端体验旁路，编排统一走 Step 即可，无功能丢失。

#### 3. 要素类型判定智能体 ✅ `a.element-kinds`
- **现状**：Step `gen_element_kinds`（steps.py:2399）。前置：知识库须有 `kb_entries.kind='element_type'`（无则 Blocked）；已判定过直接返回（天然幂等）。产出：`config.element_kinds`。next：无。
- **接入**：已完成。

#### 4. 封面智能体 ❌
- **现状**：两段 API 直调——①关键视觉蒸馏 `POST /{pid}/cover/key-visual`（LLM 据梗概/主线/前 5 角色提炼一句画面，不落库）；②生成 `POST /{pid}/cover`（画风匹配 `kb_entries(kind='prompt_block', category='style')` 取 positive + 参考图池 `config.cover_refs`，media.generate_image）。产出：`config.cover_url/cover_prompt/...`。next：无。
- **接入**（路径 B → A）：装配逻辑在端点里，需先抽出：
  1. 注册 action 工具 `project.cover_prepare`（装配提示词+参考池）到 `workflow_actions`；
  2. 新增 Step `gen_project_cover`（before=调装配、run=generate_image、next=写 `config.cover_url`），或最小改动先做成 unit：run 段 LLM 蒸馏 → 输出产物配 step。
  3. **阻碍**：回写落点是 `content_projects.config.*`，不在 `agent_next.WRITE_TARGETS` 白名单（见 §3.2）。推荐走 Step 化（产物落库归 Step.next 管，绕开白名单问题）。
- 建议 slug：`a.project-cover`，seq 插在 40 之前（如 35）。

#### 5. 项目视觉锚点智能体 ❌
- **现状**：`POST /{pid}/visual-assets/{asset_role}`（project_visual_assets.py）。三类 role：场景画风定位图 / 主要角色汇总风格定位图 / 封面海报。**硬前置**：角色汇总图需 ≥1 张角色设定图；封面海报需前两类都已存在（否则 ValueError）；依赖已发布视觉 SOP + `kb_entries(kind='skill', agent_code='artist')`。内部自带三道质检（版式规划 → 提示词质检 → 出图 → 视觉复检）。产出：`project_visual_assets` 表逐版本追加（绝不覆盖）+ 附件。next：无。前端「依次生成三张」= 串行 3 次调用、全量重出不跳过。
- **接入**（路径 A）：这是最标准的"一个智能体三个节点"案例：
  - 新增 Step `gen_visual_anchor(asset_role)` 包住 `project_visual_assets.generate`；
  - 三张各建 unit（`a.anchor-style` / `a.anchor-cast` / `a.anchor-poster`），`a.anchor-poster` 前置 `ref_flows=[a.anchor-style, a.anchor-cast]`、`dep_mode='serial'`（serial 才会 `_wait_tasks` 等前两张真正完成，海报的硬前置才成立）；`a.anchor-cast` 前置引用 `a.character-sheet`（保证 ≥1 张角色设定图）；
  - 组合卡 `c.project-anchors` 串三张，对应「依次生成三张」按钮。
  - **缺才跑语义差异**：现有按钮是"全量重出新版本、不覆盖历史"；unit 的 `only_missing` 默认只补缺。接入时该 unit 建议 `only_missing='no'`（保留"每次新版本"语义），或在按钮侧区分"补缺 / 出新版"。

#### 6. 先导预告片智能体 ❌
- **现状**：两段 API 直调——①`POST /{pid}/trailer/prompt`（全剧 5 节点硬切蒙太奇蒸馏，15 秒；默认参考图=有设定图的主要角色）；②`POST /{pid}/trailer`（`asset_gen.generate_video_asset`，Seedance 同步等待）。产出：`config.trailer_url/trailer_prompt` + 视频附件 + gen_logs。next：无。
- **接入**（路径 A）：新增 Step `gen_trailer`（before=蒸馏提示词若缺、run=generate_video_asset、next=写 config）；unit `a.project-trailer`，前置引用 `a.character-sheet`（参考图依赖角色设定图，语义上是 soft 依赖——没有也能生成，只是没参考图）。同样有 `config.*` 白名单问题 → Step 化解决。

### B. 核心要素

#### 7. 核心要素抽取智能体 ❌
- **现状**：`POST /{pid}/elements/add_ai/stream`（pipeline_stream.stream_elements，逐条落库+SSE 推送；无目录时先流式建目录）。还有非流式 `/elements` 全量建、`/elements/add` 手动加。前置：目录 + `config.element_kinds`。产出：`content_elements` + `element_appearances`。next：无。
- **接入**（路径 A 或 B）：
  - 推荐新增 Step `gen_elements`（包 `pipeline.build_elements` / `add_elements_ai` 的非流式版本），unit `a.element-extract`，前置 `ref_flows=[a.element-kinds]`。
  - 缺才跑判据：项目已有 core 要素则跳过（需给 `has_artifact` 白名单加一条项目级判据，或 unit 上 `only_missing='no'` 由组合卡控制）。
  - 前端流式按钮保留为人工旁路（编排跑非流式、页面直点跑流式，产物一致）。

#### 8. 要素档案智能体 ❌
- **现状**：`POST /elements/{eid}/profile/gen`（element_profile.py:91，道具走 gen_prop_profile）。**已被 steps.py:116 在出设定图前自动补齐**（隐式前置）。前端另有「补全缺档(N)」批量串行。产出：`content_elements.meta.profile + meta.外貌提示词`。next：无。
- **接入**（路径 B，最容易）：注册 action 工具 `element.profile_gen`；unit `a.element-profile`：开始段要 `project_id + element_id`，输出产物 `batch=project.characters`、`only_missing` 判 `meta.profile`（**需给 batch_sources 的 has_artifact 加 profile 判据**，当前判的是 sheet_url）。它是 `a.character-sheet` 的天然前置（`ref_each`）。因设定图 Step 已隐式兜底补档，此 unit 属于"显性化"而非补功能——优先级低。

#### 9/10/11. 角色 / 场景 / 道具设定图智能体 ✅（道具 ⚠️）
- **现状**：同一 Step `gen_element_sheet`（steps.py:2001）。before：缺 profile 自动补档；`sheet_fingerprint` 匹配则跳过；多形态按 variant。产出：附件 + `meta.sheet_url/sheet_fingerprint`。next：无。同时被镜级/组图 Step 的 `ensure_element_assets` 隐式补图。
- **接入**：`a.character-sheet` / `a.scene-sheet` 已有（batch 分别是 `project.characters` / `project.scenes`）。**道具无对应 batch 源**——需在 `agent_batch.SOURCES` 加 `project.props`（`ctx_key=element_id`，判据同 sheet_url），再复制一张 `a.prop-sheet`。

#### 12. 音色设计智能体 ❌
- **现状**：捏音色 `POST /elements/{eid}/voice`（voice_casting.design_voice：库内契合则复用，否则建项目级音色）；批量选角 `POST /voices/cast`（遍历未绑定角色）；发声形态标注 `/elements/annotate-voices`。产出：`meta.voice` 绑定 + `kb_entries(kind='voice')`。next：无。
- **接入**（路径 B）：注册 action 工具 `voice.design`；unit `a.element-voice`：`batch=project.characters` + 缺才跑判 `meta.voice`（需扩 has_artifact 判据）。批量选角本质就是这个 unit 的 batch 形态，可用编排替代 `/voices/cast`。

#### 13. 音色样本智能体 ✅ `a.voice-sample`
- **现状**：Step `gen_voice_samples`（steps.py:2455，批量补缺试听样本）；情绪小样是另一个 API 直调（`/admin/voices/{id}/emotion-samples`）。
- **接入**：试听样本已完成。情绪小样如需编排，注册 action `voice.emotion_samples` 即可（低优先级，管理后台功能）。

#### 14. 自由素材生成智能体 ❌
- **现状**：`POST /{pid}/assets/generate`（asset_gen.assemble_asset_prompt：`use_project`/`use_kb` 两开关，全关=纯享直达；图/视频两分支）。产出：素材库附件。next：无。
- **接入**：这是**自由创作工具而非产线节点**——没有固定前置和 next，不建议做成产线 unit。合适的形态是注册成 action 工具 `asset.generate`，供任意编排的运行段调用（模型自主出素材时用）。

### C. 剧集

#### 15. 剧集目录智能体 ❌
- **现状**：初建 `POST /{pid}/outline`（pipeline.build_outline）/ 续写 `/outline/append`（append_chapters，取末尾章接着编）/ 流式 `/outline/append/stream`（逐章落库+推送，前端唯一入口，章数留空=续 1 章、无目录=生成整本）。前置：项目基本信息（含大纲）。产出：`content_nodes(kind='chapter', title, summary)`。next：无。
- **接入**（路径 A）：新增 Step `gen_chapters`（payload 带 `count`/`instruction`，包非流式 append_chapters；首次无目录时等价 build_outline）。unit `a.chapter-append`：开始段声明自由入参 `count:int`（unit 的 params 通道已支持）；前置 `ref_flows=[a.project-outline]`；缺才跑不适用（续写本来就是增量），`only_missing='no'`。前端流式按钮保留旁路。

#### 16. 正文智能体 ❌
- **现状**：`POST /chapters/{nid}/write`（pipeline.write_chapter：检索管线装配 + 硬验收不过自动重写 ≤3 次）/ 流式 `/write/stream`（验收只警告不重写）。前置：章节点 + 数字员工章程 + 目标时长 6–10 分钟。产出：`content_bodies` + `content_nodes.summary/status='drafted'` + 要素状态回写 + `element_appearances.snapshot`。next：无（拆镜手动）。
- **接入**（路径 A）：新增 Step `gen_chapter_body`（包非流式 write_chapter——它自带重写闭环，比流式版验收更强，编排走它反而质量更好）。unit `a.chapter-write`：开始段 `node_id(章)` + 自由入参 `target_minutes:int`；前置 `ref_flows=[a.chapter-append]` 不必要（章已存在才会有 node_id），实际前置为空；next `then_flows=[a.chapter-breakdown]` 可选（打通"正文→拆镜"自动链，当前是手动断点，**接入时建议保留手动，人工审正文后再拆**）。
- 批量形态：`batch` 需要新源 `project.chapters`（判据 `content_bodies` 存在与否）——batch_sources 需扩（§3.3）。

### D. 拆镜与章级资产（全部已接 ✅，只列核对要点）

#### 17. 拆镜智能体 ✅ `a.chapter-breakdown`
- Step `breakdown_chapter`（流式逐镜 + Gate 重拆 + 总审闭环，失败回退一次性 JSON）。next：`chains_to=[expand_shot_details]`。核对：unit 若配 `then_flows=[a.chapter-expand]` 与 chains_to 重复（幂等兜住，但建议 unit 不配，让 Step 链自己跑）。`c.chapter-to-storyboard` 已因并联入队被 blocked 的坑改为 serial（sql/38），保持。

#### 18. 详细分镜展开智能体 ✅ `a.chapter-expand`
- Step `expand_shot_details`（`detail_pending` 幂等；被 gen_overview_grid / gen_stills 的 before 兜底调用）。next：`chains_to=[scene_blocking]`。

#### 19. 场景空间规划智能体 ✅ `a.chapter-blocking`
- Step `scene_blocking`（按 scene 连续段归组；组内剧本指纹缓存 168h；产出站位链 + `scene_blocking.groups` + 连续性合同 hard_locks）。sql/40 已补 `writes=content_nodes.meta.scene_blocking` 让缺才跑能判。这正是"分镜生成场景光影站位锚定文字"的实现。

#### 20/21. 空场景基准图 / 场景站位图智能体 ✅ `a.scene-empty` / `a.scene-blocking`
- Step `gen_scene_empty` / `gen_scene_sheet`（两阶段：无人基准图 → 基准图占首位+角色设定图加人）。missing_deps 反向派发已把 `scene_blocking → empty → sheet` 串起来。sql/41 已改按场景组 batch（`ctx_key=seg`）并清空 writes（**seg 不在回写白名单定位键里，这两张卡永远不能配 writes**——已知约束）。

#### 22. 整集场景组图智能体 ✅ `c.episode-scenes`
- Step `gen_scene_sheets_group`（组图 sequential，参考+生成 ≤15；before 里 `ensure_element_assets` 自动补角色档案+设定图；hard_locks 全量入词）。sql/41 已退化为纯组合（batch=''）。前端「批量场景」默认只补缺，与 Step 的 skip 语义一致。

#### 23. 定格选取智能体 ✅ `a.chapter-stills`
- Step `gen_stills`（manual，整集一次 LLM 给逐镜 first/key/last 定格）。产出仅 `meta.storyboard_still`，由装配期消费。

#### 24. 宫格故事板智能体 ✅ `a.chapter-grid`
- Step `gen_overview_grid`（PIL 预渲染格线模板 + ≤16 格/张；2026-07-12 起不再自动串 gen_prompts，纯人审参考资产）。

### E. 单镜生产（全部已接 ✅，除尾帧提示词）

#### 25. 分镜剧本智能体 ✅ `a.shot-storyboard`
- Step `gen_shot_storyboard`（redesign_shot_coverage → cuts 时间轴）。前置：连续性记录就绪（`assert_shot_generation_ready`，否则 Blocked）。是 `gen_prompts` 的 dep_kind。

#### 26. 提示词装配+质检智能体 ✅ `a.shot-prompt`
- Step `gen_prompts`（三套提示词装配 + 图十维/视频十三维并发质检，不合格自动重构复审，72h 指纹缓存）。missing_deps 自动向上派 `gen_shot_storyboard`。人工编辑打 `*_edited` 标记后装配不覆盖——编排重跑不会丢人工改动，语义安全。

#### 27. 首帧智能体 ✅ `a.shot-keyframe`
- Step `gen_keyframe`（`_shot_gen_before` 全套守卫：连续性 → 镜级要素预检补建 → 场景站位兜底 → 补设定图 → 重装配 → 质检缓存；角色硬闸 + 参考图 ≤4）。next：`_backfill_seam_for` 回填上一镜尾帧（接缝）。

#### 28. 批量首帧组图智能体 ✅ `c.episode-keyframes`
- Step `gen_keyframes_group`（≤14 镜/批、必须同 scene_seg + 同连续性合同；单镜质检不过只 skip 不拖死整批；上一批末帧作接缝参考）。前端按 GROUP_LIMIT=6 切批、默认只补缺。

#### 29. 尾帧提示词智能体 ❌（半成品）
- **现状**：端点已有 `POST /shots/{sid}/last-prompt/ai`（LLM 生成，**落库** `meta.last_image_prompt`），但前端 shotEditor 解构漏了 prop 未接线；`gen_last_keyframe` 缺提示词直接 400。
- **接入**（路径 B）：注册 action `shot.last_prompt`，unit `a.shot-lastprompt` 作为 `a.shot-lastframe` 的前置（`ref_flows`），缺才跑判 `meta.last_image_prompt`（`content_nodes.meta` 在白名单内，writes 可配）。顺手把前端接线补上（独立小任务）。

#### 30. 尾帧智能体 ✅ `a.shot-lastframe`
- Step `gen_last_keyframe`（显式清空 dep_kinds——无自动装配链，缺提示词 400 是硬前置）。接入 #29 后此链闭合。

#### 31. 分镜视频智能体 ✅ `a.shot-video` / `c.episode-video`
- Step `gen_video`（missing_deps 反向派发：缺 video_prompt→gen_prompts、缺首帧→gen_keyframe；音频预检 24h 缓存永不阻断；ARK 异步提交 poller 收割 / GRSAI 同步；输入审核拒收三级降级级联）。产出后 ffmpeg 抽首帧作封面。

### F. 整集母带实验链

#### 32/33/34/35. 母带提示词 / 母带视频 / 抽帧 / 双时间线转分镜 ❌
- **现状**：整条链完全不走 Step 队列，全在 `api/shots.py` 直调：
  - 提示词：6 种 mode（flash20 / sampled_story / timeline_supercut / adaptive_groups / adaptive_duration / slate_marked 已停用 410），产出 `meta.episode_flash_drafts`；
  - 母带：只消费服务端草稿，参考图必须与当前设定图完全一致（防漂移 409）；adaptive_groups 分组并发 + ffmpeg 拼接；产出 `meta.episode_flash_batches`，status=awaiting_review；
  - 抽帧：ffmpeg 按节拍中点抽（幂等）；
  - 双时间线：抽帧 → build_initial_timeline → 人工剪辑保存版本（乐观锁）→ 转正式分镜（不覆盖已有）。
- **接入**：**建议现阶段不接。** 理由：①链上有三个人工确认断点（草稿确认 / 母带审核 / 时间线剪辑），unit 目前没有"等待人工"节点语义；②实验性质，接口还在演化（slate 已停用）。若要接，只接第一段（母带提示词 → action 工具 `episode.flash_prompt`），后续保持人工。

### G. 成片

#### 36. 成片合成智能体 ❌（功能缺失）
- **现状**：**没有"分镜视频 → 整集成片"的端点**。唯一拼接实现 `video_concat.concat_video_attachments`（ffmpeg，统一 24fps/画幅）只被母带链调用。
- **接入**：需先实现功能：新增 Step `gen_episode_final`（before=检查本章所有镜 `video_url` 齐备、run=concat、next=写章 `meta.final_url` + 附件）。unit `a.episode-final`：前置 `ref_flows=[c.episode-video]`、`dep_mode='serial'`（要等视频真正生成完）；缺才跑判 `meta.final_url`。这是编排接入后**第一个能体现"编排补齐产线缺口"价值的新功能**。

---

## 三、接入通则与基础设施缺口

### 3.1 两条标准接入路径

- **路径 A（重生成，有装配/落库逻辑）**：把 API 端点里的装配逻辑抽成 Step 注册进 `steps.py`（before/run/next），unit 的输出产物段配 `step=<kind>` 入队——产物落库归 Step.next 管，回写白名单、缓存门禁、gen_logs 审计全部自动继承。**适用：#4 封面、#5 视觉锚点、#6 预告片、#7 要素抽取、#15 剧集目录、#16 正文、#36 成片。**
- **路径 B（轻逻辑，LLM 直出或纯取数）**：注册 action 工具进 `workflow_actions`（自动进统一工具注册表），unit 运行段 LLM 调工具 / 前置段直调，产物走 `agent_next.write_back` 白名单回写。**适用：#8 档案、#12 音色、#29 尾帧提示词、#13 情绪小样、#14 素材工具。**
- 判别标准：产物要落到**白名单外的表/列**（尤其 `content_projects.config.*`）或需要**图/视频模型调用+OSS 转存+审计**的，一律走路径 A。

### 3.2 回写白名单缺口（`agent_next.WRITE_TARGETS`）
当前只有 4 条：`content_nodes.meta` / `content_nodes.summary` / `content_elements.meta` / `content_elements.state`。
**项目级智能体（#4/5/6/15）都写 `content_projects`，白名单没有。** 两个选择：
- （推荐）项目级一律 Step 化，不碰白名单；
- 或谨慎扩白名单加 `content_projects.config`（风险：config 是大杂烩，LLM 直写容易污染）。

### 3.3 batch_sources 缺口（`agent_batch.SOURCES`）
现有 7 个源。接入上表需新增：
| 新源 | ctx_key | 缺才跑判据 | 服务的智能体 |
|---|---|---|---|
| `project.props` | element_id | `meta.sheet_url` | #11 道具设定图 |
| `project.chapters` | node_id | `content_bodies` 存在 | #16 正文批量 |
| `project.characters`（扩判据） | element_id | `meta.profile` / `meta.voice` | #8 档案、#12 音色 |
注意 has_artifact 判据目前是**每源一个固定谓词**——档案/音色和设定图共用 `project.characters` 源但判据不同，需要把判据做成源+目标可组合（小改 agent_batch）。
另：前端 `BATCH_OVER` 是静态表且已落后（缺两个 scene_groups 项），应改为从 `/api/agents/assets` 动态取（handoff 文档已记）。

### 3.4 流式端点的处理原则
流式（SSE/NDJSON）端点（大纲 / 目录 / 正文 / 要素 / 拆镜内部）是**前端体验旁路**，编排一律走非流式等价实现（正文的非流式版验收更强，还自带重写闭环）。两条路产物落点一致，功能不丢失；页面上的流式按钮全部保留。

### 3.5 人工确认断点
当前产线有意保留的手动断点（编排不应自动穿越）：
1. 正文 → 拆镜（人审正文）；
2. 母带草稿 → 母带生成 → 时间线剪辑（三个断点，见 #32-35）；
3. 宫格故事板 / 定格选取（纯人审参考物，manual Step）。
unit 目前没有"等待人工"节点语义——组合卡（`c.*`）的边界就应该切在这些断点上，**不要为了全自动串通把断点吞掉**。

### 3.6 已知机制坑（接入时逐条避开）
1. `attach:'yes'` + 配了 `step` 时会把**提示词文本**当 url 写进附件（agent_unit.py:281-286）——图/视频类 unit 配了 step 就不要配 attach。
2. `seg` 类批量（场景两件套）不能配 writes（seg 不在定位键里，sql/41 已清）。
3. `load_unit` 取**最新版本不管 status**（与 workflow 引擎取 published 相反）——草稿改动即时生效于所有引用方；产线稳定后需要决策是否改成 published 固定。
4. `then_flows` 用父 ctx 不带 batch item；且批量 `limit`（≤20）会透传给所有嵌套子编排。
5. `_wait_tasks` 只在 serial 前置生效；parallel 前置和 then_flows 都是 fire-and-forget——**下游硬依赖上游产物的，必须 serial**。
6. `test_agent_next.py` 只覆盖纯函数，`run_unit` 编排语义（嵌套/串并/批量/then）是无回归保护区——批量接入前建议先补集成测试。
7. 两套引擎并存：`workflows` 表里 30 张 unit + 2 张真 workflow 图（element-sheet / shot-prepare-elements，seq 900/910）。实际产线跑的是 unit 这套；workflow 引擎的 subflow/loop 暂不合并，收敛问题单独决策。

### 3.7 建议的落地顺序（供排期参考）
1. **P0 补链**：#29 尾帧提示词（路径 B，最小）→ #36 成片合成（新 Step，价值最大）；
2. **P1 文字上游**：#15 剧集目录、#16 正文（新 Step + unit，打通"项目 → 正文"全编排）；
3. **P1 视觉资产**：#5 视觉锚点三件套（serial 依赖是编排能力的最佳展示）→ #6 预告片 → #4 封面；
4. **P2 要素补全**：#7 要素抽取、#8 档案、#11 道具设定图、#12 音色（含 batch_sources 扩展）;
5. **暂缓**：#32-35 母带实验链、#14 素材工具化、#38 画布助手合并。

---

## 四、待决策问题

1. **版本语义**：unit 取最新版还是 published？产线跑编排后草稿即时生效是否可接受？
2. **视觉锚点的缺才跑**：编排默认"只补缺"，但锚点现有语义是"每次新版本"——按钮侧分两个入口，还是 unit 上 `only_missing='no'`？
3. **正文 → 拆镜**要不要给 `then_flows` 打通全自动（当前建议保留人工断点）？
4. **成片合成（#36）**是否确认要做？这是新功能不是迁移。
5. **母带实验链**确认不接编排、维持纯人工？
6. `content_projects.config` 是否进回写白名单（本文档建议不进，走 Step 化）？
