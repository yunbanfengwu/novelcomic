# 资产类型管理实施清单

状态：`[x]` 已完成；`[-]` 进行中；`[ ]` 未完成。

## 已完成

- [x] 明确资产类型是独立生产结果；场景、角色、道具、分镜是关联的业务主体。
- [x] 明确 type code 不限制段数，只要求全局唯一、稳定、非空并在后端以完整 code 精确注册。
- [x] 明确一个资产类型是一份标准生产契约；多个画布可以作为其首次生成、变体、重绘或批量入口。
- [x] 新增只读的管理端“资产类型”概念页和假数据颜色 Tapflow 演示画布。
- [x] 建立后端受控 `AssetTypeRegistry`；每个内置资产类型独占一个 Python 文件。
- [x] 注册初始类型：`pro.scene.brief`、`pro.scene.sheet`、`pro.character.sheet`、`pro.prop.sheet`、`pro.shot.script`、`pro.shot.keyframe`、`pro.shot.video`、`com.image`、`com.video`。
- [x] 以幂等 SQL 为 `workflow_artifacts` 增加 `asset_type`、主体和版本索引字段，并回填历史场景/关键帧等记录。
- [x] 为现有单场景设定图和分镜关键帧模板的生成节点写入 `pro.scene.sheet` / `pro.shot.keyframe`。
- [x] 新增资产类型和资产实例查询 API。
- [x] 运行写入时验证“资产类型—主体类型”契约，并为同一项目、类型、主体、变体自动递增版本。
- [x] 本地后端已重启加载迁移；已验证类型目录与项目 25 的 `pro.scene.sheet` 资产实例查询。
- [x] 通过后端 registry、单场景/分镜重点回归共 24 项测试、Python `compileall` 和前端生产构建。
- [x] 两个生产画布端到端运行通过：单场景设定图以 `pro.scene.sheet + element:265 + variant:img` 查询到产物；分镜关键帧父运行 `#232`、多要素子运行 `#233`、场景子运行 `#234` 均成功，最终产物以 `pro.shot.keyframe + content_node:1146` 查询到。
- [x] 资产类型已支持独立多标签；`media_kind` 保持为技术类型，tags 不要求包含或以 media_kind 开头。
- [x] 运行面板固定显示“智能参数”和独立的完整“实际参数”；智能参数永远只有项目、资产类型、剧集（卷/章）、分镜、素材五组下拉，不复制或按需渲染工作流真实入参。
- [x] 新增后端规范入参 Model：每个标准字段同时定义稳定键名、类型和中文名称 `label_zh`；固定选择先反查项目、卷章分镜、要素及素材等标准字段，再只匹配当前工作流公开 `input_schema`。前端不维护 `scene/shot/character/prop` 映射；未匹配的真实参数仍可手动选择或输入。

## 进行中

- [-] 将 Tapflow 节点的用户配置从兼容字段 `target_ref` / role / slot 完全收口为资产类型、操作、主体、可选变体/版本；保留 Step 仅作内部运行适配。
- [-] 将项目素材库、素材卡片到画布入口统一为 `asset_type + target_ref + canvas_role`，从资产实例精确打开画布。
- [x] 新增多要素智能生成画布：智能路由节点输出 `AssetDescriptor[]`；场景路由到 `pro.scene.sheet`，其余要素暂路由到 `com.image`。两条分支均已真实运行并可按资产类型查询。
- [x] 多要素画布使用唯一的上游 `Smart type router` 循环：每个描述符派生一组下游实例，仅执行匹配类型的节点。场景实例引用单场景设定图工作流；通用实例生成项目通用图片；两张下游卡均不是循环节点且保持图片节点尺寸。
- [x] 分镜关键帧已接入多要素智能生成工作流：数据源为显式 `source_type=shot.dynamic_elements + source_ref=content_node:<shot_id>`，后端只读取该分镜的 `element_appearances`；旧 `meta.element_ids` 仅由兼容旧工具保留，不能推断章节或项目范围。关键帧画布不再维护第二个要素循环，并保留最终关键帧节点的提示词技能、知识库、工具和 QC 配置。
- [x] 每个工作流均提供后端推导的 `output_cardinality`；被引用工作流若终止节点汇聚循环或多个产物，引用卡片自动显示为循环聚合节点，执行仍保持 `subflow`。
- [x] 父画布对子工作流仅保存引用与映射；运行态内部节点由子图和 child run 只读投影，不复制写入父图。循环产物按 descriptor 自身的 `target_ref` 落库。
- [x] 已有同类型资产的检查已前移到提示词装配之前；服务重启后的运行 `#236` 约 36ms 完成且未发起外部 LLM 请求。

## 未完成

- [ ] 为每个内置类型实现完整的主体校验、附件写入、variant/version 与读取适配器；当前已完成场景设定图和分镜关键帧兼容写入。
- [ ] 将文本产物（`pro.scene.brief`、`pro.shot.script`）写入独立资产实例和现有文本主体。
- [ ] 让素材库按资产类型读取、分组、筛选，并支持从资产实例打开/组装对应画布。
- [ ] 让生成节点直接选择输出资产类型；未选择业务落点时仅进入项目素材库。
- [ ] 实现同一资产类型的多画布入口注册、选择与回读。
- [ ] 新增角色、道具等专属生产契约后，将多要素画布的 `com.image` 临时分支替换为对应资产类型与专属画布。
- [ ] 将当前 `AssetDescriptor[]` 的 required/optional 失败策略扩展到更多资产类型和专属子流程。
- [ ] 补齐其余资产类型的迁移兼容、前后端集成和全量回归测试。
